const initialState = {
    userName : "",
    password : "",
}

const LoginFormData = (state = initialState, action) => {
    switch (action.type) {
        default: 
        return state;
    }
};

export default LoginFormData;